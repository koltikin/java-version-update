package com.cydeo.generic_tasks.Task3;


//public final class Algorithm {
//     public static <T> T max(T x, T y) {
//         return x > y ? x : y;  // objects can't compare
//     }
//}

//it causes compile error because the generics does not accept primitive


/*
Will the following class compile? If not, why?
 public final class Algorithm {
     public static <T> T max(T x, T y) {
         return x > y ? x : y;
     }
 }
 */